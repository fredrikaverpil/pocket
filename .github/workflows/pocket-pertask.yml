# Code generated by pocket. DO NOT EDIT.
# Run `./pok github-workflows` to regenerate.
# See: https://github.com/fredrikaverpil/pocket

name: pocket

on:
  push:
    branches: [main, master]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go-fix-ubuntu:
    name: go-fix (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-fix-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-fix-ubuntu-
      - name: Run go-fix
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-fix
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-fix-ubuntu-${{ hashFiles('**/go.sum') }}

  go-fix-macos:
    name: go-fix (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-fix-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-fix-macos-
      - name: Run go-fix
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-fix
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-fix-macos-${{ hashFiles('**/go.sum') }}

  go-fix-windows:
    name: go-fix (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-fix-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-fix-windows-
      - name: Run go-fix
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c go-fix
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-fix-windows-${{ hashFiles('**/go.sum') }}

  go-format-ubuntu:
    name: go-format (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-format-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-format-ubuntu-
      - name: Run go-format
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-format
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-format-ubuntu-${{ hashFiles('**/go.sum') }}

  go-format-macos:
    name: go-format (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-format-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-format-macos-
      - name: Run go-format
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-format
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-format-macos-${{ hashFiles('**/go.sum') }}

  go-format-windows:
    name: go-format (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-format-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-format-windows-
      - name: Run go-format
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c go-format
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-format-windows-${{ hashFiles('**/go.sum') }}

  go-lint-ubuntu:
    name: go-lint (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-lint-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-lint-ubuntu-
      - name: Run go-lint
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-lint
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-lint-ubuntu-${{ hashFiles('**/go.sum') }}

  go-lint-macos:
    name: go-lint (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-lint-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-lint-macos-
      - name: Run go-lint
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-lint
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-lint-macos-${{ hashFiles('**/go.sum') }}

  go-lint-windows:
    name: go-lint (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-lint-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-lint-windows-
      - name: Run go-lint
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c go-lint
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-lint-windows-${{ hashFiles('**/go.sum') }}

  go-test-ubuntu:
    name: go-test (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-test-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-test-ubuntu-
      - name: Run go-test
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-test
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-test-ubuntu-${{ hashFiles('**/go.sum') }}

  go-test-macos:
    name: go-test (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-test-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-test-macos-
      - name: Run go-test
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-test
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-test-macos-${{ hashFiles('**/go.sum') }}

  go-test-windows:
    name: go-test (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-test-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-test-windows-
      - name: Run go-test
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c go-test
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-test-windows-${{ hashFiles('**/go.sum') }}

  go-vulncheck-ubuntu:
    name: go-vulncheck (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-vulncheck-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-vulncheck-ubuntu-
      - name: Run go-vulncheck
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-vulncheck
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-vulncheck-ubuntu-${{ hashFiles('**/go.sum') }}

  go-vulncheck-macos:
    name: go-vulncheck (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-vulncheck-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-vulncheck-macos-
      - name: Run go-vulncheck
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c go-vulncheck
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-vulncheck-macos-${{ hashFiles('**/go.sum') }}

  go-vulncheck-windows:
    name: go-vulncheck (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-vulncheck-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-go-vulncheck-windows-
      - name: Run go-vulncheck
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c go-vulncheck
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-go-vulncheck-windows-${{ hashFiles('**/go.sum') }}

  md-format-ubuntu:
    name: md-format (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-md-format-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-md-format-ubuntu-
      - name: Run md-format
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c md-format
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-md-format-ubuntu-${{ hashFiles('**/go.sum') }}

  md-format-macos:
    name: md-format (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-md-format-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-md-format-macos-
      - name: Run md-format
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c md-format
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-md-format-macos-${{ hashFiles('**/go.sum') }}

  md-format-windows:
    name: md-format (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-md-format-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-md-format-windows-
      - name: Run md-format
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c md-format
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-md-format-windows-${{ hashFiles('**/go.sum') }}

  renovate-validate-ubuntu:
    name: renovate-validate (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-renovate-validate-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-renovate-validate-ubuntu-
      - name: Run renovate-validate
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c renovate-validate
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-renovate-validate-ubuntu-${{ hashFiles('**/go.sum') }}

  github-workflows-ubuntu:
    name: github-workflows (ubuntu-latest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-ubuntu-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-github-workflows-ubuntu-
      - name: Run github-workflows
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c github-workflows
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-ubuntu-${{ hashFiles('**/go.sum') }}

  github-workflows-macos:
    name: github-workflows (macos-latest)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-macos-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-github-workflows-macos-
      - name: Run github-workflows
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: ./pok -v -g -c github-workflows
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-macos-${{ hashFiles('**/go.sum') }}

  github-workflows-windows:
    name: github-workflows (windows-latest)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: .pocket/go.mod
          cache: false
      - name: Restore cache
        id: cache-restore
        uses: actions/cache/restore@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-windows-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-pocket-github-workflows-windows-
      - name: Run github-workflows
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: .\pok.ps1 -v -g -c github-workflows
      - name: Save cache
        if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
            .pocket/bin
            .pocket/tools
            .pocket/venvs
          key: ${{ runner.os }}-pocket-github-workflows-windows-${{ hashFiles('**/go.sum') }}

