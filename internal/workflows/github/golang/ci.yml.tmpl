# Generated by {{ .GeneratedBy }} - DO NOT EDIT

name: go

on:
  push:
    branches: [main]
  pull_request:

jobs:
{{- if not .SkipGoLint }}
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - name: golangci-lint
        run: ./bld go-lint
{{- end }}

{{- if not .SkipGoFormat }}
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - name: go format
        run: ./bld go-format
{{- end }}

{{- if not .SkipGoTest }}
  test:
    strategy:
      fail-fast: false
      matrix:
        os: {{ toJSON .OSVersions }}
        go: {{ toJSON .GoVersions }}
    runs-on: ${{ "{{" }} matrix.os {{ "}}" }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: ${{ "{{" }} matrix.go {{ "}}" }}
          cache: false
      - name: go test
        run: ./bld go-test
{{- end }}

{{- if not .SkipGoVulncheck }}
  vulncheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false
      - name: govulncheck
        run: ./bld go-vulncheck
{{- end }}
